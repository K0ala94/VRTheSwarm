using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraShake : MonoBehaviour {

	
	void Start () {
		
	}
	
	
	void Update () {
        /*Transform trans =GameObject.Find("Player").GetComponent<Transform>();
        float shakeX = Mathf.PerlinNoise(trans.localPosition.x-10, trans.localPosition.x+10);
        float shakeY = Mathf.PerlinNoise(trans.localPosition.y - 1, trans.localPosition.y + 1);

        trans.position = new Vector3(shakeX, shakeY, trans.position.z);*/
        
	}
}
